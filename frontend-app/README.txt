Tailwind Note:
	Do not run the command "build:tailwind" until we are ready for production (so just submission)
	Doing so before production will modify the original tailwind.css style file 
	and only include classes that are needed in our frontend codebase.

